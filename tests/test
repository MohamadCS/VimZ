const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len);
(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len);
(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len);
(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len);
(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len); const maxCunkIdx = @min(editorWin.width,chunk.len);
